[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
timeout = 60
markers =
    e2e: End-to-end tests that spawn actual CLI processes
    slow: Tests that may take longer to complete
